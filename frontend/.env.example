# Open-Pax Frontend — переменные окружения
# Скопируйте этот файл в .env и заполните значения

# Базовый URL backend API (необязательно, по умолчанию: http://localhost:8000/api)
# VITE_API_URL=http://localhost:8000/api

# Токен Mapbox БОЛЬШЕ НЕ НУЖЕН: игровая карта работает на MapLibre GL
# с офлайн-стилем (без внешних тайлов, ключей и учётных записей).
# Переменную VITE_MAPBOX_TOKEN можно удалить из своего .env
